# Plantilla: copiar a .env en esta carpeta (o exportar la variable) antes de correr scrape.py y fetch_full_archive*.py.
# Clave del proxy de scraping (ScrapeOps) usado para el archivo de Truth Social.
SCRAPE_PROXY_KEY=
